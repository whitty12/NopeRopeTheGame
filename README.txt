This program creates a game of Snake using the Pygame library.

To install:
    (1) Use pip install pygame to install the Pygame library.


TODOs:
    - Snake head should move across the screen every second or so.